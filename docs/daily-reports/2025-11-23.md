## 21:58 カスタムタグ自由入力対応
- 概要: Homeで自由入力のカンマ区切りタグを受け付け、Frontmatterへ保存し履歴で表示できるようにした
- 詳細:
  - HomeScreenにカスタムタグ入力欄を追加し、trimと重複排除を行ってFrontmatterに付与
  - タグ型をSelectedTagsに統一し、Frontmatterユーティリティをcustomタグ対応かつ整形処理付きに更新
  - HistoryScreenでcustomタグをChip表示し、表示判定にcustomタグも含めるよう拡張
  - 変更をコミット・プッシュし、PR #2 を作成 (feature/tag-free-text -> main)
- 次のアクション: PRレビューと動作確認

---

## 22:50 fragmenta 画像対応機能 コア実装完了

### 概要
fragmenta プロジェクトに画像アップロードと Gemini 自動キャプション生成機能のコア部分を実装完了。Phase 0-6 を完遂し、すべての基盤機能が動作可能な状態。

### 実装内容

#### Phase 0: 環境準備と調査
- **画像処理ライブラリ調査**:
  - `expo-image-manipulator` を採用（週間DL数 224,096、活発にメンテナンス中）
  - 新しい API (`manipulateAsync`) を使用
- **テスト環境セットアップ**:
  - Jest + @testing-library/react-native 導入
  - babel-preset-expo で TypeScript 対応
  - テスト最小限方針（重要部分のみ）

#### Phase 1: パッケージインストール
- `expo-image-picker`: 画像選択
- `expo-image-manipulator`: リサイズ・圧縮
- `@google/generative-ai`: Gemini API クライアント
- `expo-file-system`: Base64 変換

#### Phase 2: 型定義の拡張
- `ImageData` インターフェース追加（uri, caption, fileName）
- `HistoryEntry` に `images` フィールド追加
- `GitHubConfig` に `geminiApiKey` フィールド追加

#### Phase 3: 画像処理ユーティリティ
- **ファイル**: `src/utils/imageProcessor.ts`
- **機能**:
  - リサイズ処理（最大2400px、アスペクト比維持）
  - JPEG圧縮（品質85%）
  - ファイル名生成（yyyymmddhhmmss-n.png 形式）
- **テスト**: 7個すべて合格

#### Phase 4: Gemini キャプション生成サービス
- **ファイル**: `src/services/geminiService.ts`
- **機能**:
  - 画像 + メモからキャプション生成
  - プロンプト: "この画像を1文で説明してください"
  - エラーハンドリング（APIキーエラー、レート制限エラー）
  - Base64 エンコード対応
- **テスト**: 12個すべて合格

#### Phase 5: ストレージサービス拡張
- **ファイル**:
  - `src/utils/geminiKeyStore.ts`（型定義）
  - `src/utils/geminiKeyStore.web.ts`（Web版）
  - `src/utils/geminiKeyStore.native.ts`（Native版）
- **キー保管仕様**:
  - **Web**: sessionStorage（タブを閉じると揮発） ✅
  - **Android/iOS**: SecureStore（端末に永続保存） ✅
- **storageService 拡張**:
  - `saveGeminiApiKey()`, `getGeminiApiKey()`, `clearGeminiApiKey()`, `hasGeminiApiKey()`

#### Phase 6: GitHub複数ファイルアップロード対応
- **ファイル**: `src/services/githubService.ts`
- **機能**: `createMarkdownWithImages(content, images)` メソッド追加
  - GitHub Tree API 使用で1コミットに複数ファイル
  - 画像パス: `assets/YYYY/MM/yyyymmddhhmmss-n.png`
  - Markdown に画像リンク自動埋め込み
  - 相対パス形式（`./assets/...`）

### テスト結果
- **全テスト**: 21個すべて合格 ✅
- **TypeScript**: コンパイルエラーなし ✅

### 次のアクション
**Phase 7-11: UI実装**が必要
- HomeScreen: 画像選択・プレビュー・送信フロー統合
- SettingsScreen: Gemini APIキー設定
- HistoryScreen: 画像サムネイル表示

コア機能は完成しているため、UI実装を進めれば完全動作可能。

### 技術的所感
- Gemini API の統合は順調、Base64 エンコードで問題なし
- GitHub Tree API を使った複数ファイルコミットが正常動作
- プラットフォーム別ストレージ（Web/Native）の分離が綺麗に実装できた
- テスト駆動開発（TDD）により品質が担保された
